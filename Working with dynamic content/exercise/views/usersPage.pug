doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Users Page
    body
        if name.length > 0 
            ul
                each item in name 
                    li #{item}
        else 
            p No name entered 